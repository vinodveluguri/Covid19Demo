<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid_19demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PatientDetailsActivity"></activity>
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".AdminEditDetailsActivity" />
        <activity android:name=".AdminRetrieveActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".UserDietDisplayActivity" />
        <activity android:name=".UserDietDayActivity" />
        <activity android:name=".UpdateDietActivity" />
        <activity android:name=".DietActivity" />
        <activity android:name=".AdminChooseActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>